</div>
<div class="row">

  <form action="/" method="GET" >

    <% @fields.each do |field| %>
      <%= field.html %>
    <% end %>

    <%= ReconDatabase::Solver.queryer_html %>
    <%= ReconDatabase::Competition.queryer_html %>

    <button type="submit" class="btn btn-primary">Submit</button>

  </form>

  <br><br>
</div>
<div class="row">

  <table class="table table-condensed table-bordered table-striped table-hover span8">
    <tr>
      <th> Solver </th>
      <th> Event </th>
      <th> Time </th>
      <th> Competition </th>
      <th> Date Added </th>
    </tr>
    <% @solves.each do |solve| %>
      <tr>
        <td> <%= solve.solver.name %> <br> </td>
        <td> <%= solve.puzzle %> </td>
        <td> <a href="/solve/<%= solve.id %>"> <%= solve.format %> </a> </td>
        <td> <%= solve.competition.name %> </td>
        <td> <%= date_added(solve) %> </td>
      </tr>
    <% end %>
  </table>
</div>
