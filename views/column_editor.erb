<%  value = if value.is_a? Array
              value.map(&:name).join("\n")
            else
              value 
            end %>

<label><%= title %></label>
<textarea name="model[<%= field_name %>]" class="form-control"
  rows="<%= [value.to_s.lines.count, 10].min %>"><%= value %></textarea>
